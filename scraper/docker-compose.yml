services:
  scraper:
    image: lermak/marketanalyzer-scraper:latest
    container_name: marketanalyzer-scraper
    restart: unless-stopped
    env_file:
      - .env
    shm_size: "1gb"
    ports:
      - "127.0.0.1:5901:5901"