version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: marketanalyzer-backend:dev
    container_name: marketanalyzer-backend-dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"