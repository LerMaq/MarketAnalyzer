version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: marketanalyzer-frontend:dev
    container_name: marketanalyzer-frontend-dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:5173:80"